# app-bevy

Bevy-based application platform (alternative to `app`).

**Status**: Maintained as alternative implementation

Provides similar functionality to `app` crate using the Bevy game engine instead of raw OpenGL.

See `app` crate for the actively used OpenGL-based implementation.
