# core

WASM-compatible shared core types and code.

Provides:
- Input types (keyboard, mouse, controller)
- Common data structures
- Cross-platform utilities
- No platform-specific dependencies

Used across the entire Crossworld codebase for shared types.
