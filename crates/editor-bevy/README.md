# editor-bevy

Bevy-based voxel editor (alternative to `editor`).

**Status**: Maintained as alternative implementation

Provides similar functionality to `editor` crate using the Bevy game engine.

See `editor` crate for the actively used OpenGL-based implementation.
